</br>
</br>
<h1>Editer un gossip<h1>

 

    <div class="contact-box p-4 bg-primary">
            
                <%= form_with(model: @gossip) do |f| %>
                    <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
                    <input type="hidden" name="user_id">                  
                  <div class="row">                    
                    <div class="col-lg-6">
                      <div class="form-group mt-3">
                        <input class="form-control" type="text" name="title" value="<%=@gossip.title%>">
                      </div>
                    </div>
                    <div class="col-lg-12">
                      <div class="form-group mt-3">
                        <input class="form-control" type="text" name="content" value="<%=@gossip.content%>">
                      </div>
                    </div>
                    <div class="col-lg-12">
                     <button type="submit" class="btn btn-danger mt-3 mb-3 text-white border-0 py-2 px-3" ><span> Balancer mon potin ! <i class="ti-arrow-right"></i></span></button>
                    </div>
                  </div>
                <% end %>
    </div>

               <% if @gossip && @gossip.errors.any? %>
      <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
        <% @gossip.errors.full_messages.each do |message| %>
          <p class="alert alert-danger" role="alert"><%= message %></p>
        <% end %>
      <% end %>
              
    </div>